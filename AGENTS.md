# 外骨骼计划 · 仓库工作协议

> 本仓库是「AI 协作造外骨骼」项目的唯一总部（独立 git）。进入本仓库工作的 agent：先读完本文件，再读 `PROGRESS.md`（当前状态）与 `docs/决策记录.md`（为什么是现在这样）。

## 1. 项目一句话

以「AI 时代，一个人就能搞外骨骼」为主叙事的个人事业项目：10 小时/周人时预算内，12 个月造出可穿戴、可测量的单关节外骨骼，并公开全过程。细节见 `docs/需求文档.md`（章程）、`docs/三线计划.md`（计划与判据门）。

## 2. 目录结构

| 路径 | 作用 |
|---|---|
| `AGENTS.md` | 本协议 |
| `README.md` | 人类导读（薄，指向其他文档） |
| `PROGRESS.md` | 项目级看板：快照/阶段门/任务看板/本周计划/学习排期/卫星索引/周记 |
| `docs/需求文档.md` | 章程：定义/交付物/不做清单/约束/风险/待确认 |
| `docs/三线计划.md` | 计划：叙事词库/人机分工/平台流水线/阶段判据门 |
| `docs/决策记录.md` | 追加式决策日志（只增不改，最新在上） |
| `reports/` | 项目调研报告（`YYYYMMDD.标题.md`） |
| `learn/` | 学科学习（L1–L6），每科一个子目录，协议见 `learn/learn.md` |
| `content/` | 内容素材台账（INDEX + 文本条目；**只放素材，成品不放**，纪律见 §10） |
| `scripts/` | 运维脚本（daily-pm 提醒、volume_sync 外接盘备份） |
| `volume/` | 媒体成品与原始文件、文章备份（小红书/视频/录屏/截图/文章；不入 git，每日 03:40 自动同步 ext2，见 `scripts/volume_sync/`） |
| 代码工件 | 不在本仓库：网站/仿真等开工时各自独立建仓，链接登记进 PROGRESS.md 卫星索引 |

## 3. 铁律（违者按编造或越权处理）

1. **诚实第一**：不知道就说不知道；每个数字带出处；不冒充实测。
2. **安全红线**（`docs/三线计划.md` §7）不可绕过：真人穿戴实验前假人盲测、急停硬接线、高风险人群排除。
3. **词库纪律**（`docs/三线计划.md` §1.3）：对外文案不出现疗效/康复/防跌倒/医疗宣称。
4. **时间预算**：一切建议以「人在场 ≤10h/周」为约束，AI 时间不计入。
5. **判据门**：判据未过不进下一阶段；不为内容产出跳过工程判据。
6. **来源纪律**（2026-08-31）：区分「用户给的上下文」与「AI 补充的假设」——AI 在计划、参数、建议中补充的数值/限制必须显式标注来源（文献、惯例）或标「AI 设定，待确认」；不得把自己补的默认值回述成用户给定的约束（违者按编造处理）。

## 4. 常规维护动作

- **做完事必须落账**：任何推进（勾任务、新决策、新报告、学习进度）当场写入对应文件并 git 提交；周记每周日至少一行。
- **决策**：改变需求/边界/计划的，先写 `docs/决策记录.md` 再动手；反悔不删旧条目，新增覆盖性条目。
- **git**：`main` 唯一长期分支；commit 中文，格式 `<前缀>: <描述>`；前缀：`docs:` `progress:` `learn:` `report:` `content:` `code:` `clean:`。
- **双远端镜像**（2026-09-01）：gitee 与 github 同内容镜像。`origin` 配了两个 pushURL，`git push origin main` 一条命令同推两家；**禁止单独 push 单个远端**；push 后跑 `bash scripts/check-mirrors.sh` 自检一致性，不一致必须当场修复。
- **多机协作**（2026-09-01）：MBP（`~/exoskeleton`）与 mini（`~/exoskeleton`）均可编辑，双 pushURL 已各自配好。任一侧改完：`git push origin main`（一条命令同推 gitee+github）→ `bash scripts/check-mirrors.sh` 自检；**另一台开工前先 `git pull`**——尤其 mini 跑站点 sync/deploy 之前。

## 5. 「接下来做什么」协议（用户问"接下来干嘛"时）

1. 读 `PROGRESS.md`：当前快照 → 阶段门状态 → 本周计划未勾项 → 卫星索引。
2. 对照判据门：当前阶段的门是什么、还差什么。
3. 给 2–3 个候选动作，每个附：做什么、大约占用人时、为什么是它（对应哪个判据/待确认项）。
4. 不在板上的新目标不擅自加；认为该改板，先写决策记录再提。
5. 学习线建议直接看 `learn/<科目>/PROGRESS.md` 的掌握度与待深入。
6. **计划是参考非承诺**：用户提前完成本周事项时，不必等周日复盘——当场按阶段判据门与任务看板给出后续任务候选，用户确认后上板（决策记录 2026-08-30 七）。
7. **素材伴随**：建议任何工程/实践类任务时，同步给出素材点（何时录屏/拍摄/截图，见三线计划 §4.2）；命中素材库判据的当场登记进 `content/INDEX.md` 待拍区（自动记录纪律见 §10）。

## 6. 定时提醒协议（每日项目经理，2026-08-30 v2）

**触发**：launchd `local.exoskeleton-daily-pm` 每日 08:00 主触发；pi-subagents schedule `daily-pm`（every 1d，锚定 08:00）作补发兑底（晚于 08:00 才打开本项目 pi 会话时补发）。幂等：`.pi/daily-pm/stamp` 为今日则跳过；两个触发源去重。逻辑实现在 `scripts/daily-pm.sh`，测试：`DAILY_PM_FORCE=1 bash scripts/daily-pm.sh`。

**交付（2026-08-30 v2：未读会话即提醒）**：每次触发用 `pi --no-extensions -n <名称> -p <用户口吻的开场白>` 创建一条命名会话——**未读会话就是提醒本体**，Bark 推送只是手机端入口提示。用户进入回复后，该会话即转为常态会话：按 §5/落账规则当场处理（确认完成、改期、容量反馈均可落账）。行为规范：

- 每日会话（名「每日提醒 · YYYY-MM-DD」，**每天都有，含周日**）：开场白「每天早上的定时提醒到了：今天适合做什么？」，回复即下述三行建议。
- 周复盘会话（名「周复盘 · YYYY-MM-DD」，仅周日，与每日会话**并存两条**）：开场白「开始本周复盘吧……」，回复给出本周完成事项 vs 计划、与下周计划**草案**（按事规划、不记人时；周日起算，见决策记录 2026-08-30 五），用户确认后才落账改板。

定时 agent 的职责边界（会话首答同样遵守）：

- **只读**：`PROGRESS.md`（快照/阶段门/本周计划/周记末两行）+ `docs/决策记录.md` 最新一条 + `git log --since=36h`（实际进展的唯一客观证据）+ `.pi/daily-pm/last.md`（上次建议，对照执行情况）+ `content/INDEX.md` 首部库存水位行（素材饥渴提醒用，决策十五）。
- **只报**：≤3 行——「到期未勾：…｜建议今天（人的任务）：…｜判据门提醒：…」；周日追加一行复盘提醒；素材水位低于饥渴线（§10）时在三行内附一句。建议只包含人必须亲自做的任务（见三线计划 §2 分工表），不出现 AI 可独立完成的任务。
- **降级**：git 连续 ≥3 天无提交 → 不重复催促，改为问卡点/建议拆小任务/建议明确暂停；本周计划日期过期 → 提示待重排。
- **不改板**：任何 PROGRESS.md/计划改动只发生在用户在场的对话会话（周日的复盘对话）。需要改计划时输出「建议改板：…」等用户确认，由常态会话执行。

**交付**：会话 + Bark 直推（脚本自 curl，不走 agent_settled 通知链——该链按设计排除无人值守会话），同时落 `.pi/daily-pm/last.md`；日志在 `.pi/logs/`。

**不做的事**：不建每日 memory 库（实际进展以 git 提交为准，当日 AI 会话洞察由用户随手记入 `learn/<科目>/notes/` 或周记）；不引入百分比进度条（阶段门表即进度条）。

## 7. 学习进度管理

- 每科一个目录：`learn/<科目>/`（`materials/`、`notes/`、`PROGRESS.md`），协议用 `learn/learn.md`（掌握度 0–1、操作型三段式验证）。
- 开新科：建目录 → 根 `PROGRESS.md` 卫星索引与学习排期登记 → 不需决策记录条目（属执行层）。
- 排期：根 `PROGRESS.md`「学习排期」表维护；调整 = 改表 + 周记提一笔。

## 8. 出处

2026-08-29 自 pebble_space `products/exoskeleton/` 拆出为独立仓库；拆分前历史见 pebble_space（commit 299527f 及祖先）。产品开发方法论（需求→架构→UIUX…）技能库仍在 pebble_space `skills/产品开发/`，网站立项时取用。

## 9. 双机开发环境（2026-08-30）

- **mac mini（日常主力机）**：轻负载——文档、写作、浏览、仓库维护。**daily-pm（launchd 08:00 + schedule 兑底 + stamp/Bark）只部署在此机**；MBP 一律不配，避免双机重复提醒。
- **MacBook Pro（M4 Max / 128G，重负载机）**：吃配置的环境与开发放这里——MuJoCo/MJX GPU 并行仿真、批量优化、重渲染、大构建。与 mini 同 tailnet，可经 `server_debug(host=mbp)` SSH 诊断（只读向，破坏性命令会被拒）；重部署优先在 MBP 上直接开 pi 会话执行。
- 两机各自 clone 本仓库；任何跨机操作前先 `git pull` 对齐。
- **agent 不得凭空假设自己在哪台机器**（2026-09-01 补）：会话开始涉及本机操作时，先探测环境（如 `hostname` / `sysctl hw.model` / `uname -m`，M 系列重负载 + 本地已装 exo-sim 环境通常即 MBP）再行动；同时意识到用户本人不一定在本机前——可能在另一台电脑前（经 SSH/远程），或在移动端（仅接收推送、无法操作 GUI）。**判断用户位置优先看 pi-web `/api/status` 的 `net.lastRemote`**（非本机请求留痕：mode/peerName/addr/at，pi_web 6182be1 起；新鲜度自行判断，如超过 10 分钟视为过期）；拿不到或过期时再开口问。需要用户看画面/操作 GUI（viewer、OBS、扫码）时，先确认「你现在在 MBP 前、能看到屏幕吗」再启动带窗口的程序。
- 分层原则：基础 MuJoCo 验证任一机器可跑；MJX/GPU 批量与重渲染默认 MBP。

## 10. 素材库纪律（2026-08-31 决策十；2026-09-01 决策十二 v2：素材/成品分离；2026-09-03 决策十五 v3：消耗管理）

- **素材与成品分离**：`content/` 只放素材——INDEX 台账 + 文本素材条目（短的入 INDEX，长的独立 `content/YYYYMMDD-短名.md`），commit 前缀 `content:`。**成品不入本仓 content/**：媒体成品与原始文件（小红书图文/B站·YouTube 视频/录屏/截图）进 `volume/`（不入 git，每日 03:40 自动同步 `/Volumes/ext2/exoskeleton_volume`，2026-09-02 决策十四由每周日改每日，见 `scripts/volume_sync/`）；文字成品（拟发布站上的文章）先备份到 `volume/文章/`（`YYYYMMDD.{主题}.md`，2026-09-02 决策十三），再进 site 仓 `content/posts/`（进 git，写作即状态机）。
- **台账与四区结构**：`content/INDEX.md` 为唯一台账，四区——①待拍摄（提醒用）②可用弹药（文本：未用 ⬜ + 可跨平台复用 🔁 + 进行中 🟡）③已归档沉底（只检索，不建议）④文件指针；另有**成品台账**区（反向索引：成品 → 消耗清单）。顶部常驻**库存水位**行。台账只增不删，废稿标「弃」。
- **两类消耗语义**（决策十五）：S 类（截图/录屏）单用途，用一次即 ✅ 归档并附成品指针；T 类（观点/论证/叙事）可复用：⬜ 未用 → 🔁 已用@平台（**跨平台可再用、同平台不重复**）→ ✅ 用尽。同一条叙事跨平台重复出现＝品牌一致性，非浪费。
- **发布即清账（AI 职责，成品发布当天）**：①被用条目更新状态+成品指针；②成品台账加一行；③报新库存水位；④**成品反哺**——成品中的新金句/叙事点回登为 T 条目（状态直接 🔁 已用@该平台），供其他平台复用。
- **水位线**：小红书可用 <2 条或 B站 <1 条选题 → daily-pm 三行建议与周复盘附「素材饥渴」提醒；补货靠自动记录触发，不专门停工造素材（铁律 5）。
- **扫描协议**：AI 日常只扫「待拍」「可用弹药」两区；归档区仅盘点/找旧料时翻。
- **入库判据**：能回答「服务哪条产品线（B站/小红书/YouTube/网站）+ 服务哪个叙事（一个人+AI 造外骨骼）」的才入；纯过程噪音不入。
- **自动记录触发（AI 职责，无需用户要求）**：
  1. **对话中**：出现金句、完整论证、彩排过的答案、可独立成段的观点 → 当场（或会话收尾前）记为文本条目；
  2. **建议工程/实践任务时**（§5-7 素材伴随）：素材点同步登记待拍区（含触发时机+产品线）；
  3. **动手/里程碑达成时**（首次跑通、开箱、装配、首推等）：提醒拍摄，事后登记；
  4. **学习会话**：产出型讨论（选题、论证）随学随记，指针指向 learn 笔记，不重复正文；
  5. **成品反哺**：成品发布后，其中的新金句/叙事点/可复用片段回登为 T 条目（见上「发布即清账」④）。
- **边界**：AI 不碰拍摄/出镜本身；每周日复盘过一遍待拍区清库存；媒体成品产出后随即归档进 volume/（同步上盘），content/ 不留成品。
